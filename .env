"""
@description
Environment variables file for the fridge monitoring application

Key features:
- Provides placeholders for InfluxDB connection info
- Can be extended with additional environment variables as needed

@dependencies
- No direct dependencies, but other code may read from os.environ

@notes
- Real credentials or tokens should never be committed to version control
- Make sure .env is listed in .gitignore if it contains secrets
"""

# Example environment variables for local development:
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=SampleToken123
INFLUXDB_ORG=SampleOrg
INFLUXDB_BUCKET=SampleBucket
